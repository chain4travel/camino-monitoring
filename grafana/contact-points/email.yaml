# Email contact point configuration
# The __EMAIL__ placeholder will be replaced by the installer script

apiVersion: 1

contactPoints:
  - orgId: 1
    name: email
    receivers:
      - uid: email-1
        type: email
        settings:
          addresses: __EMAIL__
          subject: |
            {{ template "default.title" . }}
